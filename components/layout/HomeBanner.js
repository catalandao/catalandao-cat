import { CatDAOHeroBanner } from "@/styles/svgs/decorations";

const HomeBanner = () => (
  <div className="w-full overflow-hidden box-border">
    <CatDAOHeroBanner className="w-full max-w-max min-w-min" />
  </div>
);

export default HomeBanner;
